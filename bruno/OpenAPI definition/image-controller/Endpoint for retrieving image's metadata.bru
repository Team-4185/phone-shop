meta {
  name: Endpoint for retrieving image's metadata
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/images/:id/metadata
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
